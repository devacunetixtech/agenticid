import Header from "@/components/Header";
import AgentDirectory from "@/components/AgentDirectory";
export const metadata = { title: "Registered agents | AgenticID" };
export default function AgentsPage() { return <main className="page-shell"><Header /><AgentDirectory /></main>; }
